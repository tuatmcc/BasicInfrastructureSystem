[project]
name = "basic-infrastructure-system"
version = "0.1.0"
description = "Infrastructure management system monorepo"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
