-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.categories (
  category_id text NOT NULL,
  category_name text NOT NULL,
  CONSTRAINT categories_pkey PRIMARY KEY (category_id)
);
CREATE TABLE public.category_role (
  category_id text NOT NULL,
  role_id text NOT NULL,
  CONSTRAINT category_role_pkey PRIMARY KEY (category_id, role_id),
  CONSTRAINT category_role_access_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(category_id),
  CONSTRAINT category_role_access_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(role_id)
);
CREATE TABLE public.channel_role (
  channel_id text NOT NULL,
  role_id text NOT NULL,
  CONSTRAINT channel_role_pkey PRIMARY KEY (channel_id, role_id),
  CONSTRAINT channel_role_access_channel_id_fkey FOREIGN KEY (channel_id) REFERENCES public.channels(channel_id),
  CONSTRAINT channel_role_access_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(role_id)
);
CREATE TABLE public.channels (
  channel_id text NOT NULL,
  channel_name text NOT NULL,
  category_id text,
  CONSTRAINT channels_pkey PRIMARY KEY (channel_id),
  CONSTRAINT channels_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(category_id)
);
CREATE TABLE public.grades (
  id integer NOT NULL,
  display_grade text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT grades_pkey PRIMARY KEY (id)
);
CREATE TABLE public.members (
  name text NOT NULL,
  grade integer NOT NULL,
  emergency_contact text NOT NULL,
  student_id text NOT NULL,
  student_email text NOT NULL,
  insurance boolean NOT NULL DEFAULT false,
  some_allergy boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  member_id uuid NOT NULL DEFAULT gen_random_uuid(),
  CONSTRAINT members_pkey PRIMARY KEY (member_id),
  CONSTRAINT members_grade_fkey FOREIGN KEY (grade) REFERENCES public.grades(id)
);
CREATE TABLE public.roles (
  role_id text NOT NULL,
  role_name text NOT NULL,
  CONSTRAINT roles_pkey PRIMARY KEY (role_id)
);
CREATE TABLE public.user_role (
  discord_user_id text NOT NULL,
  role_id text NOT NULL,
  CONSTRAINT user_role_pkey PRIMARY KEY (discord_user_id, role_id),
  CONSTRAINT user_roles_discord_user_id_fkey FOREIGN KEY (discord_user_id) REFERENCES public.users(discord_user_id),
  CONSTRAINT user_roles_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(role_id)
);
CREATE TABLE public.users (
  discord_user_id text NOT NULL,
  display_name text NOT NULL,
  member_id uuid,
  CONSTRAINT users_pkey PRIMARY KEY (discord_user_id),
  CONSTRAINT users_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id)
);
