# 入部機能 TODO

## 後で検討する制限

- 学校ネットワークからのアクセスに限定するかを決める。
- 制限する場合は、学校 Wi-Fi の SSID ではなく、サーバーで判定できるグローバル IP / CIDR allowlist を使う。
- `JOIN_ALLOWED_CIDRS` のような環境変数で許可範囲を管理し、入部 API で `CF-Connecting-IP` を検証する。
- IP 制限だけでは本人確認にはならないため、必要なら招待コードや学校メールドメイン確認との併用を検討する。
